# SDV AI Service - M06 Content Processing dependencies.
#
# Pinned to the exact versions installed and verified (26/26 tests passing,
# `pip install -r requirements.txt` into a disposable venv, this task) for
# reproducibility - not guessed. Re-verify before bumping.
#
# Runtime (F-AI-001/002/007/008/003):
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
python-multipart==0.0.32
pdfminer.six==20260107
python-docx==1.2.0
openpyxl==3.1.5
puremagic==2.2.0
charset-normalizer==3.5.1
lxml==6.1.3
defusedxml==0.7.1
# M11 (F-AI-index) - /index의 Local Embedding Provider(Ollama) HTTP 호출에 쓴다.
# 이미 Test 전용으로 있던 것과 같은 버전을 그대로 Runtime으로 승격했다(새 Dependency
# 추가가 아니다 - 이미 이 requirements.txt에 있던 Library를 재사용한다).
httpx==0.28.1

# Test-only:
pytest==9.1.1
