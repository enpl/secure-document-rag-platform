# SDV MVP 우선 구현 · 보완 목록

2026-09-13 사용자 지시로 도입. 목적은 동작하는 MVP와 프론트엔드를 빠르게 완성하면서 미완료 사항을 잊지 않는 것이다. 문서 작성은 해당 기능의 구현이나 테스트 완료를 의미하지 않는다.

## 문서 위치와 관리

현재 이 파일은 Codex 관제의 원본이다. 다음 Claude 작업에서 동일 내용을 `C:/workspace/secure-document-rag-platform/docs/plan/SDV_MVP_DEFERRED.md`로 저장한다. 저장소 파일이 생성된 뒤에는 저장소 파일을 최신 원본으로 읽고 갱신하며, 이 outputs 파일은 초기 스냅샷으로 취급한다. 이전 기록을 지우지 말고 상태와 근거를 갱신한다.

Codex와 Claude는 매 작업 시작에 최신 보완 목록, 현재 Git 상태, latest.md를 먼저 읽는다. Claude는 여전히 `docs/workmd/**`에 접근하지 않는다.

## 실행 규칙

- 현재 작업에 꼭 필요한 기능과 실제로 실패하는 권한/비밀/데이터 무결성 문제를 우선 처리한다. 기능의 직접 테스트가 통과하면 해당 범위를 마친다.
- 단순 코드 취향, 추상화, 모든 가능한 예외의 선제 대응, 전 문서 재정렬은 완료를 막지 않는다.
- 이미 승인된 범위의 작은 구현 선택은 담당 에이전트가 결정하고 이유만 기록한다. 같은 설계를 반복 승인받지 않는다.
- 보완 항목의 선행 조건이 충족됐고 작은 변경으로 끝낼 수 있으면, Codex가 작업 시작 시 항목 ID·효과·수정 범위·검증·추가 비용을 한 번 제시해 사용자에게 이번에 포함할지 묻는다. 사용자가 수락하기 전에는 보완 항목을 자동으로 끼워 넣지 않는다. 기존 승인 범위의 필수 수정에는 적용하지 않는다.
- 실행자는 보완 후보를 발견하면 목록과 handoff에 남기고 승인된 작업을 계속한다. 동일한 후보를 상황 변화 없이 매번 다시 묻지 않는다.
- 직접 영향 테스트 우선. 공통 인증/스키마/토큰 경계 변경이면 Backend 회귀를 마무리 시 한 번 실행한다. 문서만 바뀐 작업은 빌드하지 않는다. 통과 후 새 변경·실패·구체적 우려가 없으면 반복하지 않는다.
- 상태를 `NOW`, `NEXT`, `BEFORE_MVP`, `BEFORE_DEPLOYMENT`, `LATER`, `CLOSED`로 구분한다. 미검증은 PASS로 계산하지 않는다.
- 단계별 합격은 범위 한정이다. UI나 Mock 테스트가 끝나도 전체 RAG/실제 Drive 완료라고 표시하지 않는다.
- 한 번에 실행 프롬프트 하나. Git 변경은 사용자만 수행하고 실행 전에 필요한 브랜치 작업을 안내한다. 매 작업 종료에 latest.md를 누적 갱신하고 다시 연다.

## 승인된 진행 순서

1. 현재 M08 브랜치에서 최소 OAuth 연결 + PostgreSQL 암호문 Token 저장 + 갱신/해제를 구현한다.
2. **M16의 프론트엔드 기반 부분을 앞당긴다.** 로그인, Source 목록/Google 연결/해제, 준비 상태부터 구현한다. M09~M15 전체 완료를 기다리지 않는다.
3. M09/M10의 동기화·메타데이터 검색이 준비될 때 문서 목록/검색 화면을 실제 API에 연결한다.
4. M11~M14의 색인·현재 권한 재확인·답변·출처 흐름을 화면에 순차 연결한다. M13의 요청 범위/Prompt 보안은 실제 질문 기능 공개 전에 적용한다.
5. 필요한 감사(M15), 실제 계정/화면 검증(M17), MVP 최종 판정(M18)을 완료한다. 고급 관리 화면은 먼저 만들 필요가 없다.

이 순서는 UI 개발 시점을 앞당긴 것이며 전체 기능 의존성이나 v1.4의 보안 계약을 삭제하지 않는다. 기존 문서의 M19/M20 검증 표기는 최신 M17/M18 체계보다 오래된 기록이다.

## 항목별 기록

| ID | 상태 | 항목 / 현재 증거 | 재검토 시점과 완료 기준 | 지금 미루는 영향 |
|---|---|---|---|---|
| MVP-01 | CLOSED | PostgreSQL 암호문 Token 저장 + 외부 주입 키. **구현 완료(Mock/Testcontainers 검증)** - `GoogleDriveOAuthController`/`GoogleDriveOAuthService`/`GoogleTokenService`/`GoogleTokenStoreAdapter`/`GoogleOAuthClient`/`GoogleOAuthStateStore`, `V007__oauth_token_store.sql`. 암호화 Round-Trip, 변조/Key 불일치/Source Reference 바꿔치기 Fail Closed, 평문 비저장, OAuth State Replay/Browser 결합/Scope 검증, Bounded Refresh, Revoke 멱등성/실패 보존을 실제 Testcontainers PostgreSQL + Local Mock HTTP Server로 검증(`GoogleTokenServiceTest` 13개, `GoogleDriveOAuthServiceTest` 7개, `GoogleOAuthStateStoreTest` 7개, 전부 통과) | 없음(구현 종결) - 실제 Google 계정 연결 자체는 MVP-03이 별도 추적한다 | 코드/DB/Mock 검증은 끝났다. 실제 계정 연결 여부는 여전히 MVP-03 완료 전까지 미검증 |
| MVP-02 | CLOSED (M16A 범위) | **M16A(2026-09-14)에서 구현 완료.** React/Vite + `keycloak-js`(Authorization Code + PKCE) 로그인/로그아웃, ADMIN 전용 Connection management(Source 등록/Google 연결/연결 해제 확인/empty·loading·error 상태/중복 제출 방지), 정직한 Home 준비 상태(가짜 Composer/답변 없음), 모바일 Off-canvas Sidebar. 신규 파일: `frontend/src/auth/*`, `frontend/src/api/*`, `frontend/src/components/*`, `frontend/src/pages/*`. Vitest+Testing Library 13개 통과, `tsc -b`/`eslint .`/`vite build` 모두 통과 | 실제 Keycloak Realm에 `sdv-frontend` Client 수동 반영 + 실제 브라우저 로그인/Google 동의 화면 왕복은 **UNVERIFIED**(아래 "현재 검증 스냅샷" 참고) - 사용자 로컬 검증 필요 | M16 전체(문서 목록/검색/답변/출처 등 M09~M15 연동)는 여전히 미완성 - 이 항목은 그 중 "프론트엔드 기반 부분"만을 가리킨다(Canonical Development Order 18 "Frontend / Guided Assistant" 전체 완료를 의미하지 않는다) |
| MVP-03 | BEFORE_MVP | 실제 Google OAuth/읽기/해제 Smoke Test 미실행. **2026-09-14 갱신(사용자 직접 확인, Agent 검증 아님)**: 사용자가 실제 Keycloak 로그인 후 실제 Google 동의 화면까지 진행했고, 등록된 Source가 "Google 계정 연결됨" 상태로 표시된 화면을 스크린샷으로 확인했다 - 로그인+최초 OAuth 연결 자체는 최소 1회 성공했다는 사용자 관찰 증거다. **여전히 미제공/PENDING**: 진단 패널을 통한 실제 파일 읽기 성공, 연결 해제/실제 Google Revoke, 재로그인 후 상태 유지, ADMIN B 교차 소유권 거부 결과 - 이 스크린샷 하나로 이들을 통과로 간주하지 않는다(`docs/runbooks/M16A_LOCAL_TESTBED.md` LIVE GOOGLE 체크리스트 5~9단계 참고) | 연결 UI와 로컬 설정 준비 시 사용자 계정으로 최소 문서 1개 검증. 실제 자격증명은 사용자 관리 | MVP-01의 코드/Mock 검증은 끝났다 - 최초 연결 성공은 사용자가 직접 관찰했다. 나머지 단계(읽기/해제/재로그인 유지/교차 소유권)의 Live 완료 주장은 여전히 불가 |
| MVP-04 | BEFORE_MVP | 전체 질문→현재 사용자 권한 확인→답변·출처 및 임시 근거 만료 미구현 | M11~M14/M17에서 실제 한 경로 완주, 권한 회수·버전 변경·근거 없음·취소 검증 | 화면 틀은 가능, 실제 답변 기능 완료는 불가 |
| MVP-05 | BEFORE_MVP | 현재 Source 소유자 전용 자격증명 결합. 다른 사용자 계정 매핑/DWD 없음 | 인증 UI 통합 전에 제품의 사용자/Source 권한 흐름 확인. 다른 사용자는 자기 자격증명 또는 명시적 미지원. 공동 Source 다사용자 시나리오 필요 시 별도 구현 | 현재 소유자 흐름 개발은 가능. 다른 사용자에 대한 owner-token 대리 사용 금지 |
| MVP-06 | CLOSED | Refresh/재연결과 disconnect 경합의 삭제 후 Token 재생성 방지(M08 자체 Writer 범위) | **M08 Writer(Refresh/최초인증/Disconnect)끼리의 경합은 실제로 동시성 테스트로 검증 완료.** `GoogleTokenService.store/revoke/publishRefreshIfStillCurrent`가 항상 부모 `source_connections`를 먼저 잠그고(`findByIdForUpdate`/`lockAndReadCurrentOwnershipState`), Refresh 발행은 `source_oauth_tokens.row_version`으로 세대(Generation)까지 확인한다. `SourceTokenConcurrencyTest`(신규, 실제 Proxied Service + 독립 Commit Transaction + Latch, Sleep 없음) 2개 - (1) Refresh가 Google 응답을 기다리는 동안 실제 `disconnect()`가 별도 Transaction으로 완전히 Commit되는 경우, (2) 최초 인증(Callback)이 응답을 기다리는 동안 Token이 전혀 없던 Source가 실제로 `disconnect()`되는 경우 - 둘 다 수정 전 코드에서 실제로 실패함을 확인한 뒤 수정 후 통과를 확인했다(Revert-Rerun-Restore로 직접 검증, 추측이 아니다) | M08 범위 안에서는 재실행 불필요(반복 요청 금지). **M11의 색인(Embedding) Writer와 disconnect의 교차 검증은 이것과 다른, 아직 열려있는 별도 항목이다 - 아래 MVP-18 참고, 여기 CLOSED에 포함되지 않는다.** |
| MVP-18 | LATER | M11 색인(Embedding) Writer와 disconnect의 교차 경합 검증 미실시 - M11 자체가 아직 존재하지 않는다 | M11 RAG Ingestion Orchestration이 실제로 `DocumentEmbeddingJpaRepository.replaceGeneration` 등을 호출하는 Writer를 만들 때, 그 Writer와 `SourceConnectionService.disconnect`의 동시 실행을 `SourceTokenConcurrencyTest`와 같은 방식(실제 Proxied Service + Latch)으로 검증한다 | MVP-06(M08 Writer끼리의 경합)과 혼동하지 않는다 - MVP-06은 이미 CLOSED, 이 항목은 M11이 생기기 전까지는 테스트 대상 자체가 없다 |
| MVP-07 | LATER | 권한 페이지 10,000 상한 직접 경계 테스트 미실행. 반복/순환은 통과 | 페이지 로직을 다음 수정할 때 작은 주입 상한으로 테스트 가능한지 제안. 현재 MVP 진행의 별도 차단 사유 아님 | 정상/순환 경로는 검증됨. 상한에서의 실패 동작 확인은 남음 |
| MVP-08 | BEFORE_MVP | Drive changeType/공유 드라이브 실제 응답 범위와 현재 file 중심 DTO | M09 Change 소비 시작 때 공식 API와 범위 대조. 공유 드라이브 이벤트를 파일 삭제로 오인하지 않고 안전하게 진행 | 현재 Mock 파일 이벤트 검증만으로 전체 Drive Sync 완료 주장 불가 |
| MVP-09 | BEFORE_DEPLOYMENT | V006 이후 과거 평문 데이터의 WAL/백업/디스크 물리적 폐기 실환경 증거 없음 | 실제 데이터 사용/배포 전 기존 V006 runbook 수행. 적용 이력·잔여 백업 범위를 증거로 남김 | 합성 개발 데이터로 UI/기능 개발 가능. 이미 민감 데이터가 있다면 즉시 상향 |
| MVP-10 | BEFORE_DEPLOYMENT | 자동 Key rotation / bulk re-encryption 도구 없음 | 기본은 active key ID·기존 키 해독·재연결 복구. 실제 고객 Token 보관 배포 전에 교체/분실/복구 절차 검증 | MVP 자동 스케줄러는 불필요. 키를 버려 기존 Token을 못 읽게 하는 운영은 금지 |
| MVP-11 | LATER | AWS Secrets Manager/KMS 또는 외부 Vault Adapter | P01/P02 등 배포 제공자가 정해질 때 기존 SourceTokenStore 경계 활용 | 로컬 MVP에 추가 서비스 없음 |
| MVP-12 | LATER | Google 대형 Workspace 문서 장시간 export/download | 실제 요구가 생길 때. 현재 한도 초과는 명시적 EXPORT_LIMIT_EXCEEDED | 큰 문서 처리 기능 제한. 잘린 원문 사용 금지 |
| MVP-13 | LATER | 고급 관리자 대시보드·일괄 운영 UX·미세 성능 최적화 | 핵심 사용자 흐름 완성 후 측정/요구에 따라 제안 | 기본 보안 검사와 서버 감사는 유지 |
| MVP-14 | BEFORE_DEPLOYMENT | 타 PC 접근·TLS·방화벽·프록시·멀티 인스턴스 OAuth state 운영 | 로컬 로그인/연결/질문이 동작한 뒤 노출 환경 확정 시 | 로컬 개발을 막지 않음. 네트워크 공개 전 검증 필요 |
| MVP-15 | LATER | 전체 명세 진행 상태/Javadoc 정렬 | 직접 수정하는 구간의 치명적 모순만 즉시, 전체 정렬은 안정화 때 | 과거 M07A 대기·100:1 승인 대기 문구를 실제 현재 상태로 오인하지 않음 |
| MVP-16 | CLOSED | M06 옛 lock-order 보정 | M07A에서 해당 평문 저장 경로 제거, 사용자 no-op 종결 | 반복 실행 금지 |
| MVP-17 | CLOSED | **M16A(2026-09-14)에서 구현 완료.** `SourceOAuthTokenJpaRepository.existsBySourceId`(부작용 없는 Spring Data 파생 조회, `SourceTokenStore.load()`/`GoogleTokenService`는 전혀 호출하지 않음) + `SourceResponse`에 `name`/`credentialPresent` 필드 추가 + `SourceConnectionService.list()`가 `SourceListItem(connection, credentialPresent)`를 반환하도록 확장(계산은 Application Service 계층에서, `SourceApiMapper`는 이미 계산된 값을 그대로 옮겨 담기만 한다 - Mapper 자신은 Repository/Service를 조회하지 않는다). 검증: `SourceConnectionServiceListCredentialPresentTest`(신규, Mockito - `credentialPresent` true/false 계산 정확성 + `SourceTokenStore`에 `verifyNoInteractions` 증명), 기존 `SourceAdminControllerWebTest` 전부 통과(Entity 필드 비노출 포함) | 없음(구현 종결) | 이전: Callback 성공/실패 Redirect 힌트만으로는 목록의 "연결됨" 표시가 부정확할 수 있었다 - 이제 서버가 실제 Credential 행 존재 여부를 직접 응답에 포함한다(단, 이는 존재 여부일 뿐 Google에서의 유효성 증거는 아니다 - Frontend/SourcesPage.tsx 주석 참고) |
| MVP-19 | BEFORE_DEPLOYMENT | 이전(Previous) Key로 암호화된 행의 복호화 자체가 코드에 없다 - `GoogleTokenService.resolveKeyForRowOrFail`은 저장된 `key_id`가 지금 설정된 활성 Key와 다르면 무조건 Fail Closed 한다(MVP-10의 "자동 Rotation 도구 없음"과는 다른, 더 근본적인 사실: 도구가 있어도 없어도 여러 Key를 동시에 읽는 경로 자체가 없다) | 실제 Key 교체(Rotation) 절차를 설계할 때, "이전 Key로 이미 암호화된 기존 행을 읽는" 경로를 명시적으로 추가할지(MVP-10과 함께), 아니면 교체 시 전량 재연결을 요구할지 결정한다 | 지금은 활성 Key 하나만 있다고 가정해도 MVP 진행에 지장 없음 - Key를 바꾸면 기존 모든 연결이 재인증 필요 상태가 된다는 사실만 운영 문서에 남는다 |
| MVP-20 | LATER | `GoogleOAuthStateStore`(진행 중인 OAuth 시도의 In-Memory 저장소)가 TTL(10분)과 "새 시도 등록 시 만료분 청소"만 있고, 저장 가능한 총 개수 상한(Count Cap)은 없다 | 실제 배포 전 대량 동시 미완료 Authorize 요청(예: 봇/공격성 트래픽)으로 인한 메모리 증가 가능성을 측정하고, 필요하면 최대 개수를 두거나 Bounded Map으로 교체한다 | 로컬/개발 단일 사용자 사용에는 지장 없음 - 배포 전 재검토 필요 |
| MVP-21 | CLOSED (Mock HTTP 경계 범위) | **M16A(2026-09-14)에서 구현 완료.** 신규 MockMvc Controller-Level 테스트 3파일: `GoogleDriveOAuthControllerAuthorizeUnconfiguredWebTest`(인증 없음→401, USER Role→403, OAuth 미설정→503 `OAUTH_UNAVAILABLE`+Set-Cookie 없음, 3개 테스트), `GoogleDriveOAuthControllerAuthorizeConfiguredWebTest`(정상 authorize→200+`authorizationUrl`+HttpOnly/SameSite=Lax/Path/Max-Age=600 Binding Cookie 발급, 1개 테스트), `GoogleDriveOAuthControllerCallbackWebTest`(거부된 동의/인식 못하는 State→400+Cookie Max-Age=0으로 즉시 제거, 2개 테스트) - 이 항목(MVP-21) 자체가 추가한 것은 정확히 **6개의 HTTP Test**다(MVP-17의 `SourceConnectionServiceListCredentialPresentTest` 1개는 별개 - **2026-09-14 정정**: 이전 기록이 "7개 신규 테스트"를 이 항목 하나에 속한 것처럼 적어 오해의 소지가 있었다. 두 항목 합쳐 7개가 맞다) | 실제 Google 계정과의 왕복(Consent 화면 실제 표시, 실제 Redirect)은 여전히 MVP-03이 별도로 추적한다 - 이 항목은 "Mock HTTP 경계(Cookie/Status/Body)가 MockMvc로 검증됨"만을 의미한다 | Application/Service 계층 로직에 더해, 이제 HTTP Filter Chain(인증/인가/Exception Handler) 경로까지 실제 Servlet Container 수준에서 검증됨 - 실제 Google 왕복만 미검증으로 남아있다 |
| MVP-23 | LATER (Live Google은 사용자 진행 대기) | **M16A follow-up(2026-09-14)에서 구현 완료.** Local Testbed 전용 1-파일 Live Read 진단(`com.sdv.testbed.*` - `TestbedReadDiagnosticController`/`Service`/DTO, `docs/runbooks/M16A_LOCAL_TESTBED.md`) - `@Profile("testbed")` + `sdv.testbed.diagnostics.enabled` 이중 게이트, 기존 `/api/admin/**` ADMIN 규칙 그대로 재사용(SecurityConfig 무변경). Mock Connector 기반 신규 10개 Backend Test 전부 통과(Profile 부재 시 미등록, 무인증/USER 거부, 교차 소유 거부(Connector 0회 호출, `verifyNoInteractions` 직접 증명), 미허용 File 거부, 비활성 Source/Credential 없음 거부, 안전한 실패 변환, 실제 Content 기반 성공/실패 구분) | 실제 Google 계정으로 이 진단을 1회 이상 성공적으로 실행하는 것은 여전히 MVP-03과 동일하게 사용자 진행 대기(UNVERIFIED) - Runbook 5절 체크리스트 참고 | Mock 검증은 끝났다 - Live Google 성공 사례 없이는 "완전한 Live 능력 증명"이라고 주장하지 않는다 |
| MVP-22 | LATER | **정정(2026-09-14, M16A)**: 이전 Handoff가 기록했던 "이 Codebase 안에서는 어떤 Network 호출 동안에도 DB Row Lock을 들고 있지 않는다"는 주장은 부정확하다 - `GoogleTokenService.revoke(Long)`는 `sourceConnectionJpaRepository.lockAndReadCurrentOwnershipState(sourceId)`(`SELECT ... FOR UPDATE`)로 부모 Source 행 Lock을 먼저 잡은 뒤에도 그 Lock을 여전히 쥔 채로 `googleOAuthClient.revokeToken(...)`(실제 Google Revoke Endpoint HTTP 호출)을 수행한다(같은 `@Transactional` 범위 안 - Lock은 Commit/Rollback까지 유지된다). `store()`/`publishRefreshIfStillCurrent()`(Refresh 발행)는 반대로 Network 호출이 끝난 뒤에만 Lock을 잡으므로 이 문제가 없다 - `revoke()`만의 예외적 패턴이다 | 실제 배포 전, Google Revoke 호출이 느려지거나(Bounded Timeout: Connect 5s/Read 10s, `GoogleOAuthRestClientConfig`) 타임아웃에 가깝게 걸릴 때 같은 Source 행에 대한 동시 Writer(예: 동시 Refresh/재인증)가 최대 그 시간만큼 대기하게 되는지 측정하고, 필요하면 Lock 범위를 좁히는 리팩터링(예: 읽을 Credential을 먼저 확정한 뒤 Google 호출은 Lock 밖에서 수행하고, 삭제만 다시 Lock 안에서 조건부로 수행)을 검토한다 - 이번 작업(M16A)에서는 M08 Token 구현 자체를 재작업하지 않는다(범위 밖) | 로컬 단일 사용자 개발에는 지장 없음(동시 경합 자체가 드묾) - 실제 다중 사용자/동시 Disconnect 트래픽이 있는 배포 전에 재검토 필요. 버그가 아니라 "Bounded지만 0은 아닌 Lock 보유 시간"이라는 사실을 정확히 기록해두는 것이 이 항목의 목적이다 |

| MVP-24 | CLOSED | **정정(2026-09-14, M16A follow-up 안전성 교정 세션)**: 기존 개발용 `sdv-keycloak` 컨테이너가 98회 이상 재시작 Loop에 빠져 있었다 - 원인은 이전 M16A 세션이 `infra/keycloak/realm-export.json`의 `sdv-frontend` Client 항목에 추가한 `"_comment"` 필드를 Keycloak의 Strict JSON Parser(`ClientRepresentation`)가 인식하지 못해 `--import-realm`이 매번 즉시 실패한 것(로그: `Unrecognized field "_comment"`). 사용자 승인 하에 그 한 줄만 제거 - 다른 Client/Role/Mapper 설정은 전혀 건드리지 않았다. Docker의 기존 `restart: unless-stopped` 정책이 다음 자동 재시도에서 스스로 정상 기동했다(수동 재시작 불필요) - 로그에 `Realm 'sdv' already exists. Import skipped`로 확인, 기존 Realm 데이터 손실 없음. `infra/testbed/realm-export.testbed.json`에도 동일 결함(Client 1개 + User 3개, 총 4곳)이 있어 테스트베드가 실제로 시작되기 전에 함께 제거했다(이 파일은 이번 작업 범위이므로 승인 절차 불필요) | 없음(구현 종결) - 두 실시간 Keycloak Realm JSON 모두 확인 완료 | 이 발견은 실제로 JSON을 Keycloak에 Import해봐야만 드러나는 종류의 결함이었다 - Node의 `JSON.parse`로는 문법 오류만 잡고 Keycloak 자신의 Schema 엄격성은 잡지 못한다는 사실을 기록해 둔다 |
| MVP-25 | CLOSED (Baseline 인프라/프로세스 범위) | M16A follow-up 안전성 교정 세션(2026-09-14) - `scripts/testbed/*.ps1`+`_lib.ps1` 재작성 완료: Secret은 Command Line에 전혀 나타나지 않음(Sentinel 값으로 직접 증명), 상속 환경변수(`SPRING_*`/`GOOGLE_*`/`KEYCLOAK_*` 등) Deny-list 제거 후 명시적 재설정, Backend/Frontend 모두 `127.0.0.1` Loopback Binding을 실제로 확인 후에만 READY 보고, PID+시작시각+실행경로 3중 식별로 프로세스 소유권 검증(가짜/재사용 PID는 절대 종료하지 않음 - 실증), Gradle Daemon이 아닌 `java -jar`(빌드된 Jar 직접 실행)로 Backend 수명 소유권 명확화. Launcher 자체의 합성 Self-Test 12개 전부 통과(`_lib.tests.ps1`) + **실제로 Testbed를 기동해 실행**: Docker Compose 격리 기동(첫 실행에서 제가 만든 결함으로 Frontend 프로세스가 미추적 상태로 남은 것을 직접 발견·정리 후 재시도, 상세는 handoff), Backend/Frontend 모두 실제 `127.0.0.1` 전용 확인, OIDC Discovery/JWKS 실접근, 미인증 호출 401 실확인(Backend 직접 + Frontend Proxy 경유 모두), `stop-testbed.ps1`으로 실제 정지 후 재시작 시 계정 자격증명/Keycloak Realm 데이터가 재사용됨을 실측(재생성되지 않음) 확인. Backend 회귀 389/389 통과(Frontend 24/24) | 실제 브라우저로 sdv-user/sdv-admin-a/sdv-admin-b 로그인 자체는 이 세션에서 **UNVERIFIED**로 남는다 - 이 환경에 브라우저 자동화 도구가 연결되어 있지 않았다(정확한 사유, 아래 handoff 참고). Password Grant 등 대체 수단을 브라우저 로그인의 증거로 쓰지 않았다(`sdv-frontend`의 `directAccessGrantsEnabled=false`도 그대로 유지). Live Google 수락은 별도로 사용자 진행 대기(UNVERIFIED) | 인프라/프로세스/보안 경계 자체는 실제로 검증됐다 - 남은 것은 "사람이 브라우저를 열어 로그인 버튼을 누르는" 단계뿐이다 |
| MVP-26 | LATER | **기록만, 미해결.** Frontend 실제 디렉터리 구조(`frontend/src/App.tsx`, `auth/`, `pages/`, `api/`, `components/`)가 v3.2 Canonical Manifest가 상정하는 것으로 보이는 `app/`, `features/`, `shared/`류 경로 관례와 다를 수 있다는 지적이 M16A follow-up 작업 지시에서 제기됐다 - 이 세션은 이를 확인/정정하지 않았다(전면 재구성 금지 지시, 범위 밖). 실제 v3.2 Frontend Manifest 경로 규칙 자체를 아직 이 저장소에서 직접 대조하지 않았다 - 다음에 Frontend 관련 작업을 할 때 `docs/spec/SDV_v3.2_FILE_MANIFEST.md`(있다면 Frontend 절)와 실제 경로를 명시적으로 대조해, 이 Drift가 실재하는지/얼마나 되는지부터 확인한다 | 이 Drift를 "이미 검토해 승인됨"으로 간주하지 않는다 - 대량 이름 변경이나 중복 Canonical 구현을 임의로 만들지 않는다 |
| MVP-27 | CLOSED (Realm Template 범위) | **M16A 좁은 교정(2026-09-14)에서 구현 완료.** 사용자가 실제 브라우저로 얻은 Access Token이 issuer/audience/ADMIN Role은 정확한데 `sub`가 없었다(만료 전이었다 - `SecurityConfig`의 `nonBlankSubjectValidator`가 이를 올바르게 거부함, 코드 결함 아님). 원인: `infra/keycloak/realm-export.json`/`infra/testbed/realm-export.testbed.json`의 `sdv-frontend` Client가 Keycloak 기본 제공 `basic` Default Client Scope(`sub` Mapper 포함)를 요청하지 않았음. 두 File 모두 `defaultClientScopes`에 `"basic"`을 추가(다른 Scope/Redirect URI/Role/`sdv-backend`는 무변경 - `KeycloakRealmExportClientScopeTest`(신규, Spring Context 없는 순수 File 내용 검사 3개)로 직접 증명: `sdv-frontend`만 `basic` 추가, `sdv-backend`는 원형 유지, `_comment` 재도입 회귀 가드 포함). `SourcesPage.tsx`의 `AUTHENTICATION_REQUIRED` 오류 문구도 "로그인이 만료되었습니다"(사실과 다를 수 있음 - 이번이 실제 사례)에서 "로그인 인증을 확인하지 못했습니다"(중립)로 정정, Vitest 1개 추가로 검증. `AGENTS.md`/`CLAUDE.md`에 `infra/testbed/secrets/google-oauth.local.env` 영구 접근 금지 조항 동기화 추가 | **이미 만든 Realm(예: 사용자가 이미 실행 중인 `sdv-testbed`)은 이 File을 고쳐도 자동으로 갱신되지 않는다** - Keycloak의 `--import-realm`은 이미 존재하는 Realm을 덮어쓰지 않는다. 사용자가 Admin Console에서 수동으로 `sdv-frontend`에 `basic` Scope를 추가하거나, 전체 초기화 후 재시작해야 기존 Realm에도 반영된다(`docs/runbooks/M16A_LOCAL_TESTBED.md` §3.5) | 이 File을 고친 것 자체는 **다음에 새로 Import되는 Realm**부터만 유효하다 - 사용자가 실제로 위 수동 절차를 수행했는지는 미확인(사용자가 직접 확인할 사항) |

새 항목을 추가할 때는 발견 근거, 실제 사용자 영향, 다음 판단 시점, 완료 증거, 최근 판단/보류 이유를 함께 적는다. 번호를 과거 기능 ID로 오인하지 않는다. 이 ID들은 작업 추적용이며 공식 File/Feature ID가 아니다.

## 현재 검증 스냅샷

- 브랜치 `feat/m08-google-drive-connector`, HEAD `59151c3`(변경 없음 - 모든 변경은 여전히 미커밋 Working Tree), 미커밋 61개 파일(수정 22 + 신규 39). 다음 실행에서 재확인한다.
- Backend 저장 결과 51개 클래스/370개 테스트 통과(50/365 → 51/370): 신규 `SourceTokenConcurrencyTest`(2개, 새 파일) + `SourceConnectionServiceAuditTransactionTest`에 1개 + `GoogleDriveOAuthServiceTest`에 2개 = 순증 5개. `SourceConnectionServiceTokenRemovalTest`의 기존 테스트 1개는 새로 정정된 disconnect() 동작에 맞게 이름/내용을 재작성했다(개수는 그대로). 실제 Google 검증은 여전히 미실행(MVP-03 그대로 유지).
- 이번(3-issue 교정) 세션에서 실제로 코드·테스트 결과가 다시 바뀌었다. 새로 추가한 각 회귀 테스트는 "수정 전 코드로 되돌려 실제로 실패하는지 확인 → 수정 복원 → 다시 통과 확인"의 Revert-Rerun-Restore 절차로 직접 검증했다(추측이 아니다) - `SourceTokenConcurrencyTest`의 Refresh-vs-Disconnect Race, `SourceConnectionServiceAuditTransactionTest`의 Unreadable-Credential Revoke, `GoogleDriveOAuthServiceTest`의 Missing-Refresh-Token 2건, 총 4개 지점 모두 확인.
- 이전 세션(M08 OAuth 최초 구현)에서 두 가지 실제 결함을 발견·수정했었다: (1) Spring Security Token Response Client에 OAuth2 전용 HttpMessageConverter를 등록하지 않으면 실제 Google 응답을 못 읽는 문제, (2) `GoogleTokenService.load()`의 Self-Invocation으로 내부 `store()` 호출이 `@Transactional`을 우회해 Pessimistic Lock이 실패하는 문제.

### M16A 세션 스냅샷(2026-09-14)

- 브랜치 `feat/m16a-frontend-foundation`, HEAD `33bb0f7`(M08 PR #31 merge, 세션 시작 시 Working Tree Clean 확인 - 이번 세션에서 변경 없음). 세션 종료 시 44개 파일(수정/삭제 20 + 신규 24) - `.claude-handoff/latest.md`에 전체 목록.
- Backend: MVP-17(existsBySourceId+SourceResponse 확장)/MVP-21(Controller HTTP 경계 3개 신규 파일) 구현. Docker Desktop이 세션 시작 시 꺼져 있어 직접 기동 후 전체 회귀 실행 - **377개 클래스/테스트 전부 통과, 0 실패/0 오류**(이전 370 → 377, 순증 7: `SourceConnectionServiceListCredentialPresentTest` 1 + `GoogleDriveOAuthControllerAuthorizeUnconfiguredWebTest` 3 + `GoogleDriveOAuthControllerAuthorizeConfiguredWebTest` 1 + `GoogleDriveOAuthControllerCallbackWebTest` 2).
- Frontend: 최초로 실제 애플리케이션 코드 작성(`keycloak-js`+`react-router-dom` 신규 의존성, Vitest+Testing Library 신규 개발 의존성). `npx tsc -b`/`npx eslint .`/`npx vitest run`(13/13)/`npm run build` 전부 통과. 실제 Keycloak/Google 브라우저 왕복은 **UNVERIFIED**(사용자 로컬 설정 필요 - `.claude-handoff/latest.md`에 정확한 수동 설정 절차 기록).
- MVP-22(신규) - `GoogleTokenService.revoke()`가 Google Revoke Network 호출 동안 부모 Source Row Lock을 실제로 들고 있음을 코드 확인으로 정정 기록(이전 Handoff의 "Network 호출 중 Lock을 전혀 들고 있지 않다"는 주장은 부정확했다 - 위 표 참고).

### M16A follow-up 세션 스냅샷(2026-09-14, 실제 OIDC/Google 테스트베드)

- 브랜치 `feat/m16a-frontend-foundation`, HEAD `33bb0f7`(변경 없음). **테스트 카운트 사실관계 정정**: 직전 M16A 세션 종료 시 보고했던 "377/377 전체 통과"는 실제로 실행되고 관찰된 결과였으나, 그 직후 좁은 범위 재확인 실행(`--tests "com.sdv.source.*"`)이 `build/test-results/test/`의 XML을 16개 클래스/141개 테스트로 덮어써 디스크 상의 증거 자체가 그 좁은 실행 결과로 대체됐다(Codex가 발견) - 이는 "실패를 감춘 것"이 아니라 "더 넓은 실행의 디스크 증거가 이후의 더 좁은 실행으로 물리적으로 대체된 것"이다. 그 전체 통과 자체를 허위라고 주장하지도, 반대로 검증됐다고 재주장하지도 않는다 - 이번 세션에서 새 변경사항(Testbed 진단 포함) 이후 **전체 회귀를 다시 실행해 새 디스크 증거로 대체**했다(아래).
- 이번 세션 최종 전체 회귀: **387개 테스트, 0 실패/0 오류, 57개 Test 파일**(직전 377 → 387, 순증 10 = `com.sdv.testbed.*` 신규 2개 파일: `TestbedReadDiagnosticControllerWebTest` 9개 + `TestbedReadDiagnosticControllerAbsentWebTest` 1개). 이 값은 `backend/build/test-results/test/*.xml`에서 직접 재집계했다(추정이 아니다).
- **직전 세션 표현 정정**: 그 세션이 실제로 추가한 것은 "1개 Service-Layer 단위 테스트(MVP-17) + 6개 Controller HTTP 테스트(MVP-21)"였다 - 위 MVP-21 표 항목을 "7개 신규 테스트"라고만 적어, 마치 그 7개가 전부 MVP-21(OAuth HTTP) 소속인 것처럼 읽힐 수 있었다. 실제로는 7개 중 1개(MVP-17)는 OAuth HTTP 테스트가 아니다 - 위 MVP-21 행에서 이미 정정했다.
- Frontend: `SourcesPage.tsx`의 Google Callback 배너가 URL 정리 직후 사라지던 버그를 수정(`callbackHint`를 Mount 시점 State로 분리) + 여러 Fetch가 겹칠 때 오래된 응답이 최신 목록을 덮어쓰지 않도록 요청 순번 Guard 추가. Vitest 20/20 통과, `tsc -b`/`eslint`/`vite build` 모두 통과.
- 신규 격리 Testbed 인프라(`infra/testbed/*`, `scripts/testbed/*.ps1`, `docs/runbooks/M16A_LOCAL_TESTBED.md`) - 기존 `infra/docker-compose.dev.yml`/Keycloak Realm `sdv`/기존 Postgres 볼륨을 전혀 건드리지 않는, 완전히 별도의 Compose 프로젝트(`sdv-testbed`, 포트 15173/18080/18180/15432)로 구현했다. 이 세션은 `docker compose ... config`로 정적 검증만 수행했고, 실제로 Testbed Compose를 기동하거나 Keycloak Admin API를 호출하지 않았다(사용자가 직접 `start-testbed.ps1`을 실행해야 한다) - Baseline/Live Google 수락은 모두 **UNVERIFIED**로 남는다.

## 공부용 메모

Token은 Google API용 출입증이고 Master Key는 저장된 출입증을 잠그고 여는 열쇠다. DB에는 잠긴 출입증만 두고 열쇠는 실행 환경에서 별도로 제공한다. Frontend에는 Google Token을 전달하지 않고 연결 성공 여부만 보여준다.
